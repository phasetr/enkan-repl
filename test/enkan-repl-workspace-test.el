;;; enkan-repl-workspace-test.el --- Tests for workspace utilities -*- lexical-binding: t -*-

;;; Commentary:
;; Test suite for enkan-repl-workspace.el pure functions

;;; Code:

(require 'ert)
(require 'enkan-repl-workspace)

;; test-enkan-repl--generate-next-workspace-id is defined in
;; enkan-repl-workspace-management-test.el to avoid duplication

;; test-enkan-repl--add-workspace is defined in
;; enkan-repl-workspace-management-test.el to avoid duplication

;; test-enkan-repl--get-workspace-state is defined in
;; enkan-repl-workspace-management-test.el to avoid duplication

;; test-enkan-repl--can-delete-workspace is defined in
;; enkan-repl-workspace-management-test.el to avoid duplication

;; test-enkan-repl--delete-workspace is defined in
;; enkan-repl-workspace-management-test.el to avoid duplication

;; test-enkan-repl--list-workspace-ids is defined in
;; enkan-repl-workspace-management-test.el to avoid duplication

(provide 'enkan-repl-workspace-test)
;;; enkan-repl-workspace-test.el ends here